/*
function add(x : number, y : number) : number {
    return x + y;
}

interface Emp {
    id : number,
    name : string,
    salary : number
}

var emp : Emp = {
    id : 100,
    name : 'Magesh',
    salary : 1000
};

console.log(add(10,20));
*/
function add(x, y) {
    return x + y;
}
add(10, 20);
